import java.io.*;
import java.util.*

fun readInts() = readLine().split(' ').map{ it.toInt() }

fun main() = with(BufferedReader(InputStreamReader(System.`in`))){
    var attribute = readInts()
    val stack = ArrayDeque()


}
